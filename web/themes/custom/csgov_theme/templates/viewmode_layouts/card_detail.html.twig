{#
/**
* card detail. Same as card simple but with added summary.
*
* List of regions: title, info, summary, tags, image.
*/
#}

{%
  set classes = [
  'card',
  'card--detail',
]
%}

<div {{ attributes.addClass(classes) }}>
  {{ title_suffix.contextual_links }}

  {% block image %}
    {% if content.image|render|striptags('<img>')|trim %}
      <div class="card__image">
        {{ content.image }}
      </div>
    {% endif %}
  {% endblock %}

  {% block content %}
    {% if content %}
      <div class="card__content">

        {% block title %}
          {% if content.title|render|striptags|trim %}
            <h3 class="card__title">
              {{ content.title }}
            </h3>
          {% endif %}
        {% endblock %}

        {% block info %}
          {% if content.info|render|striptags|trim %}
            <div class="card__info">
              {{ content.info }}
            </div>
          {% endif %}
        {% endblock %}

        {% block summary %}
          {% if content.summary|render|striptags|trim %}
            <div class="card__summary">
              {{ content.summary }}
            </div>
          {% endif %}
        {% endblock %}

        {% block tags %}
          {% if content.tags|render|striptags|trim %}
            <div class="card__tags">
              {{ content.tags }}
            </div>
          {% endif %}
        {% endblock %}

      </div>
    {% endif %}
  {% endblock %}

</div>
